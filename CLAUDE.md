# コーディングルール

- コードの変更はできるだけ最小限にする。依頼された変更に直接関係しない箇所は触らない。
- 不要になったコード（未使用の関数・変数・CSSクラスなど）は残さず、その場ですぐに削除する。
- コメントの方針:
  - 変数宣言や値の設定など、読めば分かる単純な処理にはコメント不要。
  - ライブラリ／パッケージの関数を使っている箇所や、複雑な計算・ロジックでコードだけでは意図が分かりにくい行にはコメントを付ける。
